{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "docker-build": "sh -c 'docker build -t camcloud/${0} -f ./packages/${0}/Dockerfile .'"
  },
  "devDependencies": {
    "lerna": "^3.16.4"
  }
}
